Items: ${item_names} 
User: ${user_name} 
Write in English language message informing ${user_name} their items (use short names only, 
without any quantity or size like "2L", "250ml", etc.) are being prepared, and please wait.

Strict Rules: 
*Use only short names of items.
*Extract and use only the first name from ${user_name}. 
*If the extracted name appears real human, use a respectful tone; otherwise, skip honorifics. 
*Tone must be friendly and professional, like a restaurant assistant. 
*If any items are clearly premade (e.g., Soda, Ice Cream, Coke, Water, Sprite), or anything that is premade, combine them in a grouped phrase like:
• "Your Sprite and Ice Cream will be right with you..."
*If there are more than 5 items, do not name them — just say the order is being prepared.
*Return only the message in the English language, characters — no translations, explanations, or extra text.