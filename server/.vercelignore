# Docker
Dockerfile
.dockerignore
docker-compose.yml
Docker-CMD-Server.md

# Local env files (set these in Vercel Dashboard instead)
.env
.env.production

# Migrations (run manually or via a separate script)
migrations/

# Dev docs
*.md

# Local dev only
.vscode/
