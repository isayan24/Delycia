Priority Fix:  
  ☐ i want you to make another login system in #client side
    - this will be my backup system for user login
    - the feature will be
      - suppose a user scans qr code -> opened teh menu -> select item -> while placing order -> there will be a phone number field(text will be like, the number is needed for parsing the order) , and a name input field (text will be like, the name is needed for taking the order to the person).
      - and after succcessfully ordered with the phn number and name -> the user will be logged in with that details automatically
      - this system is for when my whatsapp otp will be ended i'll start this service
      - so build a shortcut in the codebase for #client, like if i comment the whatsapp magic link login and uncomment the no otp/magic link login -> the service will start working immidiately, else no code changing needed for running the service...
    ____________________________________________________ 
    first analyze my app architecture, see how the data is flowing, and storing to the database you have to analyze my app full structure in these codebases #client #server
    _________________________________________________
    then implement the system as a senior developer youre, and follow the production ready approach #tanstack
  

  ☐ Fix variant deletion not working on a existing item
  ☐ in the orders history page, the timing showing its wrong!!
   

Todo: 
  Cart:
    ☐ Create rid column in the cart table
    ☐ Sync cart when user has logged in: sync with the redux
 

  Client-Side:
    ☐ Add preparation time for order if not exist
    ☐ Fixed the scanner issue, means if there is no table stored in the cookie.. then a scanner can be oppened after clicking on the scanner btn

Features:  

  Super Admin Site:
    ☐ Add a site for super admin to manage all the delycia restaurants
    ☐ From there i can crud restaurant owners as well!
 
   
  Delycia Wallet:
    ☐ Add wallet to the user profile
    ☐ This will be user specific wallet so no need to in the admin
      
  Explore Delycias:
    ☐ This feature is for user site that shows a map and show delycia restaurants
    ☐ This will increase the user engagement 
    ☐ Include search functionality for searching delycia restaurant or specific location name
    