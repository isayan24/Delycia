
Todo:
   Priority Fix:    
      
    ☐ Fix when site is in sleep, add a btn to refresh the site for latest token flow and user detection

  Inventory:
    ☐ Fix variants are not there when adding new bulk item
    ☐ Fix the tab switch in bulk inventory adding: that working reverse
    ☐ 
  Cart:
    ☐ Create rid column in the cart table
    ☐ Sync cart when user has logged in: sync with the redux

Features: 
  Overview Page:
    ☐ This page is all about a quick overview of some stuff
    ☐ First make a backend route and functions for this, then this gonna be easy
    ☐ In the overview show:
      - Sales: The sales of the restaurant, filter based sales(today, this month, this year)
      - Inventory: Show the inventory items, which stocks are very low, show the total nos of them
      - Customers: Show the total nos of customers, filter based customers(today, this month, this year)
      - 
    ☐ Make a Page: Reports, that will have sales report and inventory report
      - In the inventory report will have the complete report of every item, including specific inventory item sales, what perform better, what perform worse, that perticular item ordered how many persons etc.
      - In the sales report will have: Move the whole dashboard page to here actually


  Delycia Wallet:
    ☐ Add wallet to the user profile
    ☐ This will be user specific wallet so no need to in the admin
    ☐ 
  Explore Delycias:
    ☐ This feature is for user site that shows a map and show delycia restaurants
    ☐ This will increase the user engagement 
    ☐ Include search functionality for searching delycia restaurant or specific location name
    ☐ 


     catch (err) {
      console.error('Error submitting form:', err)

      let errorMessage = 'Error updating item or variants'

      if (axios.isAxiosError(err) && err.response?.data?.message) {
        errorMessage = err.response.data.message
      }

      showError('Error', errorMessage)
    }