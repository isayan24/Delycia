
Todo:

  Plans Table:
    ☐ Create a table for user plans expiration date
    ☐ in the table these columns are needed id, user_id, start_date, end_date, plan_type:(free, premium, pro), status:(active, inactive, expired), created_at, updated_at
    

  Stock:
    ☐ If user ordered, and if the order is canceled, then the stock should be the same as before 

  Tables:
    ☐ In tables and-customer order site, add a field to select how much customers are ordering in the table @started(26-02-03 08:58)
      - For this i have to create a new column in the orders table as peoples, -> and update this accordingly
 

   Priority Fix: 
    ☐ Middleware: if there is no user, only the login page is accesible -> do it
  
    
  Cart:
    ☐ Create rid column in the cart table
    ☐ Sync cart when user has logged in: sync with the redux
 


Features:  

  Super Admin Site:
    ☐ Add a site for super admin to manage all the delycia restaurants
    ☐ From there i can crud restaurant owners as well!

  Notification System:
    ☐ Add a notification system for admin and staffs
    ☐ Create a slider based or sidebar to the right side, from here notifications will store
    ☐ Notifications like stock alerts, new order, order canceled, staff online etc

   
  Delycia Wallet:
    ☐ Add wallet to the user profile
    ☐ This will be user specific wallet so no need to in the admin
      
  Explore Delycias:
    ☐ This feature is for user site that shows a map and show delycia restaurants
    ☐ This will increase the user engagement 
    ☐ Include search functionality for searching delycia restaurant or specific location name
    


     catch (err) {
      console.error('Error submitting form:', err)

      let errorMessage = 'Error updating item or variants'

      if (axios.isAxiosError(err) && err.response?.data?.message) {
        errorMessage = err.response.data.message
      }

      showError('Error', errorMessage)
    }